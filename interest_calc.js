var principal, rate, years, interest;

principal = prompt("Input principal amount");
rate = prompt("Input interest rate");
years = prompt("Input number of years on the loan");

interest = principal * rate * years;

console.log(interest);

